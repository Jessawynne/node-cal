#!/usr/bin/env node --harmony_destructuring

'use strict';

//const [,,...args] = process.argv;
//// ignores the first two arguments in the array; gives you just the command line arguments
////console.log(args);

//if (args.length === 2) {
  //const [month, year] = args;

  //console.log('generateMonth(${year}, ${month})');//${} => template string
  //// in terminal: ./cal.js a b
//} else if (args.length === 1) {
  //const [year] = args;

  //console.log('generateYear(${year})');
  //// in terminal: ./cal.js a
//} else {
  //console.log('nope broke it');
  //// in terminal: ./cal.js a b c
  //process.exit(64);
//};

//if (year < 1753) {
  //console.log('cal : App does not work prior to 1753);
//};

//if (year > 9999 || year < 1) {
  //console.log('cal : Year not in range 1..9999');
//};


